{
  "name": "rag-avatar-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild frontend/bundle.js --bundle --minify --format=iife --global-name=Avatar --outfile=app/static/avatar.bundle.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dompurify": "^3.2.6",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.3"
  }
}
