{
  "name": "rag-avatar-frontend",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild frontend/markdown-renderer.js --bundle --minify --format=iife --global-name=AvatarMarkdown --outfile=app/static/markdown-renderer.bundle.js",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "dompurify": "^3.2.6",
    "markdown-it": "^14.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.25.5",
    "jsdom": "^26.1.0",
    "vitest": "^3.2.3"
  }
}
